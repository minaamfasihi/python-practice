# methods are functions built into objects
# you can think of them as having an argument 'self' referring to the object itself

l = [1, 2, 3, 4]
l.append(6)

print(l)

print(l.count(3)) # counts how many times 3 appears in list

help(l.count) # help function spits out info about its arguments

